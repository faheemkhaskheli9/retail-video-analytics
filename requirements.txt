# Core runtime dependencies for the Phase 1/2 MVP pipeline.
opencv-python-headless>=4.9,<5
numpy>=1.26,<3
PyYAML>=6.0,<7

# Dev/test
pytest>=8.0,<9

# Optional: only needed if configs/*.yaml set detector.backend: yolo
# ultralytics>=8.1,<9
