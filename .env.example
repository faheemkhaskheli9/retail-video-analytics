# Copy to .env and fill in.
# Not currently read by the pipeline (all runtime settings live in
# configs/*.yaml) -- kept as a placeholder for the future API/dashboard
# phase, which will likely need a database connection string.
DATABASE_URL=
